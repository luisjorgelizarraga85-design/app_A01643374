name: app_a01643374
description: App para contar células en imágenes de microscopio usando IA.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Selección de imágenes desde galería o cámara
  image_picker: ^1.0.7
  
  # Peticiones HTTP (se usará en Fase 2 con Roboflow)
  http: ^1.2.0
  
  # Íconos adicionales
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
